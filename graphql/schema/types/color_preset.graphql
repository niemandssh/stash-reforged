type ColorPreset {
  id: ID!
  name: String!
  color: String!
  sort: Int!
  tag_requirements_description: String
  required_for_requirements: Boolean!
  created_at: Time!
  updated_at: Time!
}

input ColorPresetCreateInput {
  name: String!
  color: String!
  sort: Int
  tag_requirements_description: String
  required_for_requirements: Boolean
}

input ColorPresetUpdateInput {
  id: ID!
  name: String
  color: String
  sort: Int
  tag_requirements_description: String
  required_for_requirements: Boolean
}

input ColorPresetDestroyInput {
  id: ID!
}

type FindColorPresetsResultType {
  count: Int!
  color_presets: [ColorPreset!]!
}
